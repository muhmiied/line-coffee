

# دليل واجهة المستخدم التفصيلي — Line Coffee

> هذا الملف هو **وصف تفصيلي ممل** لكل شاشة وكل عنصر مرئي في مشروع Line Coffee. الهدف: أن يستطيع أي مطوّر أو AI agent يقرأ هذا الملف أن يعيد بناء **نفس الشكل، نفس الكلمات، نفس الترتيب، نفس الإفكتات، نفس الموشن، ونفس المكتبات** بدون الرجوع للكود الأصلي.
>
> الملف مقسوم إلى:
> 1. مرجع سريع (Stack + الألوان + الخطوط).
> 2. وصف الـ Layout المشترك (الموجود في كل صفحة).
> 3. وصف كل صفحة على حدة — كلمة كلمة، كارت كارت.
> 4. أوديت عام في الآخر عن الإفكتات والموشن والأشكال المتكررة.
>
> **ملاحظة لغوية:** المشروع ثنائي اللغة (عربي/إنجليزي) مع دعم RTL كامل. كل نص ظاهر مكتوب بدالة `t('English', 'العربية')` التي تختار النص حسب اللغة الحالية. سأكتب النصّين معاً في كل موضع.

---

## القسم 0 — مرجع سريع (Tech Stack)

| البند | القيمة |
|------|--------|
| Framework | Next.js 16 (App Router) + React 19 |
| اللغة | TypeScript |
| الستايل | Tailwind CSS v4 (لا يوجد `tailwind.config.js` — كل شيء في `globals.css` عبر `@theme inline`) |
| الموشن | **framer-motion** (المحرّك الأساسي للموشن) + CSS `@keyframes` للأنميشن المتكرر |
| الأيقونات | **lucide-react** فقط (لا توجد إيموجي كأيقونات) |
| الكومبوننتس | shadcn/ui (نسخة مخصّصة جداً بالألوان الذهبية) |
| الإشعارات | **sonner** (Toaster) |
| State | Zustand (`lib/store/cart.ts`, `lib/store/wishlist.ts`) + React Context (`auth`, `language`) |
| الخطوط | Playfair Display (عناوين) · Cairo (نصوص عربية) · Inter (نصوص إنجليزية) |

### الهوية اللونية في سطر واحد
مود **سينمائي داكن (Dark Cinematic) بطابع القهوة الفاخرة**: خلفيات بنية شبه سوداء، ولمسات ذهبية نحاسية (Copper Gold) كلون أساسي، ونصوص بيج كريمي دافئ. لا يوجد أبيض نقي ولا أسود نقي تقريباً — كل شيء "دافئ".

### الألوان الحرفية (المستخدمة مباشرة في الكود بصيغة HEX/RGBA)
```
الذهبي الأساسي (Copper):     #B6885E
الذهبي الفاتح (Highlight):    #D6A373
البيج الكريمي (نص أساسي):     #F5E6D8
البيج الباهت (نص ثانوي):      #D6B79A  /  rgba(183,155,133,..)
خلفية الموقع الأغمق:          #070504  (الفوتر)
خلفية داكنة (دروبداون/قوائم): rgba(18,13,9,0.95)
لون شريط الإعلان (بني غامق):  #3d1a00  نصّه #FFDCC2
```
هذه القيم تتكرر **حرفياً** في كل المشروع عبر `style={{...}}` و Tailwind arbitrary values مثل `text-[#D6A373]` و `bg-[#B6885E]/10`.

---

## القسم 1 — الـ Layout المشترك (يظهر في كل الصفحات)

ترتيب الطبقات من فوق لتحت في `app/layout.tsx`:

```
<html dir="rtl/ltr"> (يتغيّر حسب اللغة)
  <body> خلفية بتدرّج مزدوج داكن
    1. PageLoader        ← شاشة تحميل سينمائية (أول زيارة) + شريط تقدّم رفيع (التنقل)
    2. StickyTopBar      ← يحتوي: [شريط الإعلان المتحرك] + [الهيدر]
    3. {children}        ← محتوى الصفحة
    4. Footer            ← الفوتر (يختفي في صفحات الأدمن)
    5. CartDrawer        ← درج السلة (ينزلق من الجنب)
    6. WishlistDrawer    ← درج المفضلة
    7. Toaster (sonner)  ← الإشعارات المنبثقة
    8. WhatsAppButton    ← زر واتساب عائم
```

### 1.1 — شاشة التحميل السينمائية (PageLoader)

**الملف:** `components/layout/page-loader.tsx` · **المكتبة:** framer-motion

لها وضعان:

**أ) أول زيارة (Cinematic — مدّتها 2400ms):** تظهر مرّة واحدة لكل جلسة (تُخزَّن في `sessionStorage` بمفتاح `line-coffee-initial-load`). المكوّنات بالترتيب من الخلف للأمام:
- **خلفية إسبريسو غامقة** (`.loader-espresso-bg`) تغطي الشاشة كاملة.
- **Vignette دائري** (تظليل الأطراف) `.loader-vignette`.
- **توهّج كهرماني دافئ** خلف اللوجو يظهر تدريجياً (`opacity: [0, 1, 0.75, 1]` على مدى 85% من الوقت).
- **سقف ضوئي دافئ** أعلى الشاشة بارتفاع 32%.
- **خصلات بخار واقعية (Steam Wisps):** ثلاث خصلات بخار ترتفع من أعلى اللوجو، كل واحدة بإزاحة أفقية ومدّة تأخير مختلفة. كل خصلة تتحرك: تظهر شفافيتها `[0 → 0.28 → 0.17 → 0]`، يتمدّد ارتفاعها، وترتفع لأعلى مع تمايل أفقي بسيط. المدّة 4.2 ثانية بتكرار لا نهائي.
- **اللوجو الملوّن** (`/brand/logo-colored.svg`) يظهر بـ `scale: 0.92 → 1` و `y: 8 → 0` مع خاتم توهّج خلفه.
- **خط ذهبي رفيع** يتمدّد أفقياً (`scaleX: 0 → 1`).
- **تاجلاين:** كلمة `Crafted with Precision` بخط Serif مكتوب بحروف كابيتال، يظهر بعد تأخير 0.9s.
- **شريط تقدّم سفلي ذهبي** يمتلئ من 0 إلى 100% على مدى مدّة اللودر.

منحنى الحركة الموحّد لكل هذه الأنميشن: `cubic-bezier(0.22, 1, 0.36, 1)` (ease-out ناعم سينمائي).

**ب) التنقّل بين الصفحات (Nav — 700ms):** مجرّد **شريط رفيع ذهبي أعلى الشاشة** يمتلئ أفقياً `scaleX: 0 → 1`. لا توجد شاشة كاملة.

### 1.2 — شريط الإعلان المتحرك (StickyTopBar / AnnouncementBar)

**الملفات:** `components/layout/sticky-top-bar.tsx` + `announcement-bar.tsx`

- شريط مثبّت أعلى الصفحة (`fixed top-0 z-50`)، ارتفاعه الأدنى `38px`.
- الخلفية: `#120D09` بشفافية 95% مع حدّ سفلي ذهبي `border-[#B6885E]/15` وظل خفيف.
- النص الافتراضي (عربي): `🚀 توصيل مجاني على الطلبات فوق 200 ج`.
- يجلب المحتوى من `/api/settings/announcement`.
- **إفكت لمعان (Shimmer) دائم:** طبقة تدرّج بيج شفّاف `via-[#FFDCC2]/12` تتحرك أفقياً من `-120%` إلى `240%` كل 5.5 ثانية بشكل لا نهائي (تنطفئ لو المستخدم مفعّل "تقليل الحركة").
- **وضعان للعرض:**
  - `fade`: الرسائل تتبدّل واحدة تلو الأخرى بتأثير `opacity + y` كل عدّة ثوانٍ (افتراضي 4000ms).
  - `marquee`: شريط نصّي يتحرك أفقياً بشكل مستمر `x: 0% → -50%` (سرعة خطّية)، مع فاصل نقطة ذهبية `•` بين الرسائل.
- زر **X** للإغلاق على اليمين (يخفي الشريط للجلسة الحالية).
- يختفي تماماً في صفحات `/dashboard/admin`.

### 1.3 — الهيدر (Header)

**الملف:** `components/layout/header.tsx` · **المكتبة:** framer-motion + lucide-react

**السلوك العام:**
- ارتفاعه: `56px` موبايل → `64px` → `96px` ديسكتوب.
- **حالتان:** شفّاف فوق صفحات معيّنة (`/`, `/products`, `/about`, `/products/*`) ويتحوّل لـ **زجاجي (nav-glass)** عند النزول أكثر من 20px أو في باقي الصفحات.
- في الحالة الزجاجية تُضاف 3 طبقات زخرفية: مسحة ذهبية أفقي�� خفيفة، خط ذهبي علوي رفيع، ولمعان داخلي علوي. في الحالة الشفّافة يُضاف تظليل أسود متدرّج من فوق + خط ذهبي رفيع تحت المنتصف.

**المحتوى (يسار → يمين في LTR):**
1. **اللوجو** (`/brand/logo-white.svg`): يدخل بأنميشن `opacity: 0→1, x: -16→0`. أبعاده تكبر مع الشاشة.
2. **روابط التنقّل (ديسكتوب فقط):** `Home/الرئيسية` · `Products/المنتجات` · `About/من نحن` · `Contact/تواصل معنا` · `Blog/المدونة`. كل رابط:
   - لونه `#D6B79A/85` ويصبح `#F5E6D8` عند الـ hover.
   - تحته **خط ذهبي متدرّج** ينمو من العرض 0 إلى كامل العرض عند الـ hover (`w-0 group-hover:w-full`)، والرابط النشط يكون خطّه كامل مع توهّج `shadow-[0_0_14px_rgba(214,163,115,0.45)]`.
   - يوجد إفكت `nav-sweep` على النص.
3. **مجموعة الأزرار (يمين):**
   - **زر اللغة** (EN/AR) دائري مع سهم لأسفل ChevronDown، يفتح دروبداون داكن زجاجي.
   - **زر البحث** (أيقونة Search): يفتح **بانل بحث منبثق** بأنميشن `opacity + y + scale`، خلفيته تدرّج داكن مع `backdrop-blur(24px)` وحدّ ذهبي. يبحث فوري في المنتجات (مجلوبة من `/api/products?limit=120`). يظهر النتائج كأزرار، أو "لا توجد منتجات / No products found".
   - **NotificationCenter** (مركز الإشعارات — أيقونة جرس).
   - **زر المفضلة** (Heart): فيه **شارة عدّاد** دائرية بتدرّج ذهبي `linear-gradient(135deg, #B6885E, #D6A373)` تظهر بأنميشن `scale: 0→1`.
   - **زر السلة** (ShoppingBag): نفس فكرة شارة العدّاد الذهبية.
   - **قائمة المستخدم:** لو مسجّل دخول → اسمه + دروبداون (Dashboard/My Account, Edit Profile, Orders, Wishlist, Sign Out). لو أدمن يظهر له روابط مختلفة. زر Sign Out لونه أحمر. لو غير مسجّل → أيقونة User تنقل لـ `/auth/login`.
   - **زر قائمة الموبايل** (Menu/X) يظهر تحت `md`.
- **كل أزرار الأيقونات** لها hover موحّد: `hover:bg-[#B6885E]/10 hover:text-[#F5E6D8]` + توهّج `hover:shadow-[0_0_24px_rgba(182,136,94,0.14)]`.

**قائمة الموبايل (Mobile Menu):** عند الفتح:
- طبقة سوداء معتمة خلفية `rgba(0,0,0,0.7)` مع `blur(4px)`.
- لوحة تنزلق من الجنب (يمين في LTR، يسار في RTL) بأنميشن **spring** (`damping: 25, stiffness: 200`)، خلفيتها `rgba(15,10,7,0.97)` مع `blur(20px)` وحدّ ذهبي.
- بداخلها: حقل بحث، ثم روابط التنقّل (كل رابط يدخل متدرّجاً بـ `delay: index * 0.08`)، فاصل ذهبي، روابط الحساب، وزر اللغة.

### 1.4 — درج السلة (CartDrawer)

**الملف:** `components/cart/cart-drawer.tsx`

- يفتح بالضغط على أيقونة السلة. خلفية معتمة `bg-foreground/20 backdrop-blur-sm`.
- الدرج ينزلق من الجنب بأنميشن **spring** (`damping: 25, stiffness: 200`)، عرضه حتى `28rem`، خلفيته `bg-card`.
- **الهيدر:** أيقونة ShoppingBag ذهبية + عنوان `Your Cart / سلتك` (خط Serif) + عدد المنتجات + زر X.
- **لو فارغة:** دائرة بها أيقونة حقيبة باهتة + `Your cart is empty / سلتك فارغة` + نص ترحيبي + زر `Browse Products / تصفح المنتجات`.
- **عناصر السلة:** كل عنصر كارت بخلفية `bg-secondary/50` وحدّ خفيف، يدخل بأنميشن `opacity + y`، ويخرج بانزلاق أفقي. يحتوي: صورة المنتج (64–80px)، الاسم، الحجم، وتفاصيل توليفة الإسبريسو (لو موجودة) بنسب الحبوب، أزرار الكمية (Minus/Plus دائرية)، السعر، وزر حذف (Trash2 أحمر عند hover). أزرار الكمية تتحقق من المخزون وتُظهر toast خطأ لو تجاوز.
- **الفوتر:** ملخص (المجموع الفرعي، الشحن، تنبيه "أضف X ج.م لتوصيل مجاني"، الإجمالي) + زر **Checkout / إتمام الطلب** (له hover يرفعه لأعلى مع ظل ذهبي) + زر **Continue Shopping / متابعة التسوق**.

### 1.5 — درج المفضلة (WishlistDrawer)

نفس فكرة درج السلة (انزلاق spring من الجنب، خلفية معتمة) لكن لعرض المنتجات المحفوظة، مع إمكانية النقل إلى السلة أو الحذف.

### 1.6 — الفوتر (Footer)

**الملف:** `components/layout/footer.tsx`

- خلفية `#070504` (أغمق نقطة في الموقع). فوقها طبقة توهّج ذهبي بيضاوي من المنتصف العلوي، وخط ذهبي رفيع أعلى الفوتر.
- **شبكة من 6 أعمدة** (lg):
  - **عمود العلامة (عمودان):** اللوجو الأبيض + نبذة وصفية بيج باهت + 4 أيقونات سوشيال (Instagram, Facebook, TikTok, YouTube) دائرية بخلفية ذهبية خفيفة. الأيقونات تتغيّر للذهبي عند الـ hover عبر `onMouseEnter/Leave` يدوي.
  - **الفئات / الفئات:** Turkish Blends, Espresso Blends, Easy Coffee, Flavor Coffee (تُجلب من `/api/categories`).
  - **اصنع منتجك / Make Your Product:** Make Your Espresso, Make Your Flavor.
  - **الشركة / Company:** About Us, Contact, Blog, Our Story, Sourcing.
  - **تواصل / Contact:** العنوان (MapPin), الهاتف (Phone), الإيميل (Mail) — أيقونات ذهبية.
  - كل الروابط لونها بيج باهت وتتحوّل ذهبي `hover:text-[#D6A373]` عند الـ hover.
- **الشريط السفلي:** حقوق النشر `© [السنة] Line Coffee. All rights reserved / جميع الحقوق محفوظة` + روابط (Privacy Policy, Terms of Use).
- **يختفي تماماً** في صفحات `/dashboard/admin`.

### 1.7 — زر واتساب العائم (WhatsAppButton)

زر دائري عائم ثابت في زاوية الشاشة بلون واتساب، للتواصل السريع.

---

## القسم 2 — الصفحة الرئيسية (Homepage) — `app/page.tsx`

الصفحة تُبنى من 9 سكاشن بترتيب قابل للتحكم من الأدمن، لكن الترتيب الافتراضي:
`hero → categories → features → story → best_sellers → blog → testimonials → instagram → contact`

كل سكشن مستقل في ملفه داخل `components/home/`. أغلب السكاشن تجلب بياناتها من API لكن لها بيانات احتياطية (fallback) ثابتة لو الـ API فشل — سأذكر الـ fallback لأنه ما يظهر افتراضياً.

### مفاتيح الموشن المتكررة (مُعرَّفة في `motion-primitives.tsx`)
هذه المكوّنات تُستخدم في معظم السكاشن — احفظها لأنها لغة الموشن للموقع كله:
- **`SectionReveal`**: عنصر `<section>` يدخل عند ظهوره بـ `opacity: 0→1, y: 40→0` خلال `0.7s ease-out`، ويُفعّل تتابع (stagger) لأبنائه بفاصل `0.15s`. يحدث **مرّة واحدة** (`once: true`) عند ظهور 10% منه.
- **`FadeUp`**: ابن يدخل `opacity: 0→1, y: 40→0` خلال `0.6s`.
- **`ImageReveal`**: ابن صورة يدخل `opacity: 0→1, scale: 1.05→1` خلال `0.7s` (تكبير عكسي ناعم).
- **`StaggerContainer`**: حاوية تتابع الأبناء.
- **`WordByWord`**: يقسّم العنوان لكلمات، كل كلمة تدخل `opacity: 0→1, y: 28��0` بفاصل `0.1s` بينها (تأثير "ظهور كلمة كلمة").
- **منحنى الحركة السينمائي المتكرر:** `cubic-bezier(0.25, 0.1, 0.25, 1)` و `cubic-bezier(0.22, 1, 0.36, 1)`.

### 2.1 — قسم البطل (Hero) — `hero-section.tsx`

- **الحاوية:** ارتفاع `86svh` (موبايل) / `86vh` (ديسكتوب)، خلفية `#0B0806`. تمتد تحت الهيدر (margin سالب) ليكون الهيدر شفّافاً فوقها.
- **سلايدر** من شرائح (يجلب من `/api/media?section_key=hero`، وله 4 شرائح احتياطية). يتبدّل تلقائياً كل 6 ثوانٍ.
- **خلفية كل شريحة** (`HeroBackground`): صورة `object-cover` فوقها **6 طبقات سينمائية بالترتيب**:
  1. تظليل أساسي غامق (`rgba(0,0,0,0.52)`).
  2. Vignette دائري (الأطراف غامقة، المنتصف مفتوح).
  3. رفع سفلي (`bottom lift`) يدمج الهيرو مع السكشن اللي تحته بلون `#0B0806`.
  4. تدرّج جانبي (يُغمّق خلف النص للتباين) — اتجاهه يعكس مع RTL.
  5. توهّج ذهبي محيطي (`radial-gradient ... rgba(182,136,94,0.14)`).
  6. حبيبات فيلم (Film grain) عبر SVG اختياري.
- **بارالاكس:** الخلفية تتحرك `y: 0% → 30%` مع التمرير، والمحتوى يتلاشى `opacity: 1 → 0`.
- **حبة قهوة زخرفية** (SVG) في الزاوية السفلية اليسرى بشفافية 20% ودوران `-12deg`.
- **تبديل الشرائح:** الخلفية تتلاشى (crossfade `0.8s`)، والمحتوى يدخل/يخرج بـ `opacity + y` (`0.38s`).
- **محتوى الشريحة (محاذٍ لليسار في LTR، لليمين في RTL):**
  - **Eyebrow:** نص صغير بحروف كابيتال متباعدة `tracking-[0.24em]` لون `#D6A373` (مثال: `Signature Roasts / تحميصات مميزة`).
  - **العنوان (h1):** خط Serif غليظ جداً، حجم متجاوب `clamp(2.8rem, 5.4vw, 5.8rem)`، لون `#F5E6D8`، مع ظل نص قوي. مقصوص لـ 3 أسطر.
  - **العنوان الفرعي:** نص بيج `rgba(214,183,154,0.95)` مقصوص سطرين.
  - **زرّان CTA:**
    - الأساسي: خلفية تدرّج ذهبي `linear-gradient(135deg, #B6885E, #D6A373)`، نص داكن `#0B0806`، ظل ذهبي. عند الـ hover يرتفع `translateY(-1px)` ويزيد توهّج الظل. بداخله سهم `ArrowRight` يتحرك يميناً عند الـ hover (ويُقلب في RTL). النص الافتراضي `Shop Coffee / تسوق القهوة`.
    - الثانوي: زجاجي شفّاف بحدّ ذهبي و`backdrop-blur(8px)`، نص `Our Story / قصتنا`.
  - **صف إحصائيات (Stats):** 3 أرقام فوق خط ذهبي علوي، كل رقم بخط Serif ذهبي مع عدّاد متحرّك (`AnimatedCounter`) وتسمية تحته. (أمثلة: `15+ Origins Curated`, `72h Fresh Roast Window`, `100% Arabica Focus`).
- ملاحظة: كل عناصر الهيرو (eyebrow/title/subtitle/buttons/stats) قابلة للإخفاء والتحريك من محرّر الأدمن.

### 2.2 — قسم الفئات (Categories) — `categories-section.tsx`

- **الحاوية:** `SectionReveal`، خلفية `#0F0A07`، padding رأسي `80–112px`. كلاس `cinematic-section`.
- **طبقات الخلفية:** توهّجان ذهبيان بيضاويان (أعلى وأسفل) + خطّان ذهبيان رفيعان (أعلى وأسفل) بتدرّج `via-[#B6885E]/20`.
- **الهيدر (وسط):** Eyebrow بين خطّين ذهبيين قصيرين: `Browse by Category / تصفح حسب الفئة` لون `#D6A373`. تحته عنوان `Shop by Category / تسوق حسب الفئة` بكلاس `premium-heading-shimmer` (لمعان متحرك) + `WordByWord`.
- **الشبكة:** `grid` متجاوبة (عمودان موبايل → حتى **7 أعمدة** على XL)، فجوة `12–16px`.
- **كارت الفئة** (`premium-image-card`): نسبة `3/4`، حواف `rounded-xl`. يحتوي:
  - صورة خلفية `object-cover` بفلتر سينمائي `brightness-[0.82] contrast-[1.08] saturate-[1.04]`، تكبر `scale-110` على مدى `700ms` عند الـ hover.
  - 4 طبقات تظليل: أسود يخفّ عند الـ hover، صبغة دافئة، تدرّج سفلي، وخاتم ذهبي يظهر عند الـ hover.
  - **شارة "جديد / NEW"** (لفئات "اصنع منتجك") بتدرّج ذهبي مع أيقونة Sparkles.
  - **اسم الفئة** (خط Serif، وسط) + سطر `Explore / استكشف` (أو `Create Blend / اصنع التوليفة`) مع سهم يتباعد عند الـ hover.
- **حالة التحميل:** 7 مربعات skeleton بـ `animate-pulse` خلفية ذهبية شفّافة.

### 2.3 — شريط المميزات (Features Pills) — `features-pills.tsx`

- **الحاوية:** `SectionReveal`، خلفية `#0B0806`، خطّان ذهبيان أعلى وأسفل + توهّج مركزي.
- **شبكة** من 2 (موبايل) إلى 4 أعمدة، تتابع `StaggerContainer`.
- **كل ميزة** (`premium-info-card`، عمود متمركز):
  - دائرة `56–64px` بخلفية ذهبية خفيفة وحدّ ذهبي، بداخلها أيقونة lucide (سُمك خط `1.5`) لون `#B6885E`، تكبر `scale-105` عند الـ hover.
  - عنوان بيج يتحوّل ذهبي عند الـ hover + وصف صغير باهت.
  - **الافتراضي (4 مميزات):** `24/7 Support / دعم على مدار الساعة` · `Fast Delivery / توصيل سريع` · `Freshly Roasted / تحميص طازج` · `Premium Quality / حفظ الجودة`.

### 2.4 — قسم القصة (Story) — `story-section.tsx`

- **الحاوية:** خلفية `#0F0A07`، padding كبير `96–144px`، كلاس `cinematic-section`.
- **خلفية:** صورة مزرعة قهوة بشفافية 10% بحركة **بارالاكس** (`y: 20% → -20%`) + تظليل غامق + توهّجان ذهبيان (يسار للمحتوى، يمين للصورة) + خطّان ذهبيان.
- **شبكة عمودين** (lg):
  - **عمود المحتوى** (يدخل بانزلاق أفقي `x: ±50 → 0`): Eyebrow `Our Story / قصتنا` (كلاس `premium-section-kicker`) + عنوان ضخم `premium-heading-shimmer` + فقرة وصفية + **قائمة قيم** (كل قيمة: دائرة أيقونة ذهبية تكبر عند hover + عنوان + وصف، تدخل بتأخير متتابع `index*0.15`) + زر `premium-button-outline` دائري `Learn More About Us / تعرف علينا أكثر` مع سهم.
    - القيم الافتراضية: `Sustainably Sourced / مصادر مستدامة` · `Expert Roasting / تحميص احترافي` · `Passion for Quality / شغف بالجودة`.
  - **عمود الصورة** (يدخل بانزلاق أفقي معاكس): إطار `aspect-[3/4]` بحواف `rounded-2xl`، حوله **هالة ضوئية** (`blur-3xl`) وإطار خفيف. الصورة بفلتر سينمائي + تكبير خفيف عند الـ hover + طبقات تظليل ووهج وحبيبات. **كارت إحصائيات عائم** (`luxury-panel`) في الزاوية السفلية يدخل بـ `scale: 0.88 → 1`، يحتوي حتى 3 أرقام ذهبية مفصولة بخطوط رأسية.

### 2.5 — قسم الأكثر مبيعاً (Best Sellers) — `best-sellers-section.tsx`

- **الحاوية:** خلفية `#0d0600`، صورة خلفية اختيارية من الأدمن + توهّج دافئ مركزي (`blur-[80px]`) + تلاشي علوي وسفلي يدمج السكشن بجيرانه.
- **الهيدر:** Eyebrow `Top Picks / الأكثر اختياراً` + عنوان `Best Sellers / الأكثر مبيعاً` (`premium-heading-shimmer`، لون `#FFDCC2`) + على اليمين رابط `View All Best Sellers / عرض الأكثر مبيعاً` مع سهم.
- **شبكة منتجات:** عمودان (موبايل) / 4 أعمدة (lg)، تتابع `staggerChildren: 0.1`. تعرض حتى 4 منتجات `is_best_seller` عبر `ProductCard`.
- **التحميل:** 4 كروت skeleton (`luxury-panel`، `animate-pulse`).

### 2.6 — بطاقة المنتج (ProductCard) — `product-card.tsx` (مكوّن مفصلي ومتكرر)

أهم مكوّن متكرر في الموقع. التركيب:
- **القشرة** (`luxury-card`): حواف `rounded-xl`، خلفية تدرّج عمودي `from-[#1B140F] via-[#15100B] to-[#0B0806]`، حدّ ذهبي `border-[#B6885E]/16` يتوهّج لـ `#D6A373/34` عند الـ hover، ظل ناعم.
- **منطقة الصورة** (ارتفاع `128–176px`): صورة بفلتر سينمائي `brightness-[0.82] contrast-[1.08] saturate-[1.05]` تكبر `scale-[1.08]` على `700ms` عند الـ hover. لو مفيش صورة → **CinematicPlaceholder** (تدرّج حسب الفئة + أيقونة حقيبة باهتة + توهّج سفلي). فوقها طبقات تظليل (vignette سفلي + صبغة بنية دافئة `mix-blend-multiply` + توهّج علوي عند الـ hover).
  - **شارات أعلى يسار** (مكدّسة): `New / جديد` (بيج)، `Best Seller / الأكثر مبيعاً` (بني)، `Sale / تخفيض` (أحمر) — كلها كبسولات صغيرة بحروف كابيتال.
  - **زر المفضلة أعلى يمين:** دائري زجاجي، يظهر عند الـ hover (ديسكتوب) ودائم (موبايل). القلب يمتلئ ذهبي لو في المفضلة. ينبض `scale-110` عند الضغط.
  - **زر "إضافة سريعة / Quick Add"** (`premium-button`): ينزلق من الأسفل ويظهر عند الـ hover، كبسولة بأيقونة حقيبة. يضيف حجم 250g افتراضياً ويُظهر toast نجاح.
  - **حالة نفاد المخزون:** طبقة سوداء + كبسولة `Sold Out / نفد المخزون`. وحالة المخزون المنخفض: كبسولة كهرمانية `Only X left / X قطع فقط`.
- **منطقة المعلومات:** اسم (خط Serif، سطر واحد، يتفتّح عند الـ hover) + وصف (سطرين) + **شبكة 3 أحجام** (`250g/500g/1kg`) كل خانة كبسولة بالحجم + السعر بالذهبي `X EGP / ج.م` (الأحجام غير المتاحة باهتة).

### 2.7 — قسم المدوّنة (Blog) — `blog-section.tsx`

- خلفية `#0F0A07` + توهّج علوي + خطّ ذهبي. هيدر: Eyebrow `Coffee Journal / مجلة القهوة` + عنوان `Latest From The Blog / أحدث المقالات` + رابط `View all posts / عرض كل المقالات`.
- **3 كروت مقالات** (يجلب من `/api/blog/public`): كل كارت حواف `rounded-2xl`، حدّ ذهبي خفيف، يرتفع `-translate-y-1` عند الـ hover. يحتوي صورة غلاف (تكبر عند الـ hover) + تاريخ بأيقونة Calendar + عنوان (خط Serif يتحوّل ذهبي) + مقتطف (3 أسطر، تُنزع منه وسوم HTML).

### 2.8 — قسم آراء العملاء (Testimonials) — `testimonials-section.tsx`

- **الحاوية:** `SectionReveal`، خلفية `#0B0806` (مع صورة اختيارية من الأدمن وتأثيراتها) + توهّج علوي + خطّان ذهبيان.
- **الهيدر:** Eyebrow `Customer Notes / آراء العملاء` + عنوان `Loved in Everyday Rituals / قهوة أحبها عملاؤنا...` + زرّان: `premium-button` دائري `Browse Menu / تصفح القائمة` + زر شفّاف `Leave a Review / اترك تقييمك`.
- **3 كروت آراء** (يجلب من `/api/testimonials?featured=true`): كل كارت `min-h-[300px]`، حواف `rounded-2xl`، خلفية `#120D09/78` مع `backdrop-blur`، يرتفع ويتوهّج عند الـ hover (`hover:-translate-y-1` + ظل ذهبي). يحتوي: **توهّج زاوية علوي** (`blur-3xl`)، صف **5 نجوم** (`Star` ممتلئة ذهبي حسب التقييم) + أيقونة اقتباس `Quote`، نص الرأي (5 أسطر)، وفي الأسفل أفاتار العميل (أو حروف أولى داخل دائرة بتدرّج إشعاعي) + الاسم (خط Serif) + النوع/المدينة.

### 2.9 — قسم إنستجرام (Instagram) — `instagram-section.tsx`

- خلفية `#0F0A07` + توهّج + خطّ ذهبي. هيدر: Eyebrow `@linecoffee.eg` + عنوان `Follow Our Journey / تابع رحلتنا` (`WordByWord`) + رابطا Instagram و Facebook بأيقونات.
- **شبكة 6 صور مربّعة** (حتى 6 أعمدة على lg)، كل صورة `premium-image-card`: تكبر `scale-110` عند الـ hover، وتظهر فوقها أيقونة Instagram بيضاء بتوهّج + خاتم ذهبي.

### 2.10 — قسم التواصل (Contact) — `contact-section.tsx`

- **الحاوية:** `SectionReveal`، خلفية `#0B0806` + توهّج + خطّان ذهبيان. شبكة عمودين (lg):
  - **عمود المعلومات:** Eyebrow `Get In Touch / تواصل معنا` + عنوان `We'd Love To Hear From You / نحب أن نسمع منك` (`WordByWord`) + فقرة + **4 عناصر تواصل** (كل عنصر: دائرة أيقونة ذهبية + عنوان + قيمة): الموقع (MapPin)، الهاتف/واتساب (Phone — رابط wa.me)، الإيميل (Mail)، ساعات العمل (Clock).
  - **عمود الفورم** (`luxury-panel`، `rounded-2xl`): عنوان `Send Us a Message / أرسل لنا رسالة` + فورم (الاسم، الإيميل، الموضوع، الرسالة) كلها حقول بخلفية ذهبية شفّافة `rgba(182,136,94,0.07)` وحدّ ذهبي + زر `premium-button` دائري `Send Message / إرسال الرسالة` (يصبح `Sending... / جارٍ الإرسال...` أثناء الإرسال). يرسل لـ `/api/contact` ثم يفتح واتساب، ويُظهر toast.

---

## القسم 3 — صفحات المنتجات

### 3.1 — صفحة قائمة المنتجات — `app/products/page.tsx` (1460 سطر، أضخم صفحة)

ملف ضخم يضم القائمة + 3 أدوات تخصيص تفاعلية. خلفية الصفحة `#0B0806`. مغلّفة بـ `<Suspense>` (لأنها تقرأ `searchParams`) مع fallback بسيط (هيرو فارغ بمستطيل ذهبي باهت).

- **ProductsHero** (أعلى الصفحة): بانر هيرو يجلب صورته من `/api/media?section_key=products_banner` (له صورة احتياطية). يمتد تحت الهيدر (margin سالب).
- **التخطيط:** `flex` عمودي على الموبايل، أفقي على `lg`: **شريط جانبي** بعرض `64` (`lg:w-64`) + **منطقة رئيسية** مرنة.
- **الشريط الجانبي (Sidebar):** بطاقة `luxury-panel` لاصقة `sticky top-28` بحواف `rounded-2xl`. عنوان `Categories / الفئات` (خط Serif) + قائمة أزرار فئات:
  - الزر العادي: شفّاف، نص بيج باهت، عند الـ hover يكبر `scale-[1.01]` ويظهر حدّ وخلفية ذهبية خفيفة.
  - **الزر النشط:** خلفية **تدرّج ذهبي** `linear-gradient(135deg, #B6885E, #D6A373)`، نص داكن `#0B0806`، ظل ذهبي، يكبر `scale-[1.02]`، وبه سهم `ChevronRight` في النهاية.
  - أزرار "اصنع" (make-your-espresso / make-your-flavor) لها **حدّ متقطّع منقّط** (`border-dashed`) وأيقونة `Sparkles`.
  - الفئة الافتراضية عند الدخول: `turkish-blends`. تغيير الفئة يحدّث الـ URL (`?category=`) بدون تمرير.
- **المنطقة الرئيسية:** حسب الفئة المختارة تعرض إما:
  - **أداة "اصنع إسبريسو"** (`MakeYourEspressoBlend`) — انظر 3.2.
  - **أداة "اصنع نكهتك"** (`CustomizeFlavor`).
  - **أو القائمة العادية:** حقل بحث (أيقونة `Search` يسار + خلفية داكنة + حدّ ذهبي عند التركيز) + سطر `Showing N products / عرض N منتج` + **شبكة منتجات** (عمودان موبايل / 3 أعمدة `lg`، فجوة `12–20px`) من `ProductCard`.
  - **حالة التحميل:** 6 كروت skeleton (`luxury-card`، `animate-pulse`).
  - **حالة الفراغ:** أيقونة `PackageSearch` باهتة + عنوان `No products found / لم يتم العثور على منتجات` + زر `Back to Turkish Blends / الرجوع إلى توليفات تركي`.

### 3.2 — أداة "اصنع إسبريسو" (Make Your Espresso) — أهم تجربة تفاعلية

أداة تكوين توليفة قهوة حية بذكاء. تستخدم القشرة المشتركة `PremiumConfiguratorShell` (انظر 3.4).
- **مبدّل وضعين** (شبكة كبسولتين بإطار ذهبي): `Quick / سريع` (نسب ذكية تلقائية) أو `Custom / مخصص` (تتحكم بالنسب يدوياً).
- **قسم "رشح لي / Guide Me"** (`rounded-2xl`، حدّ ذهبي): أيقونة `Sparkles` + خيارات تفضيلات (البروفايل، القوام، أرابيكا فقط، مراعاة الميزانية) + زر `premium-button` يطبّق توليفة مقترحة.
- **شبكة حبوب البن** مقسّمة لمجموعتين (`Arabica` / `Robusta`)، كل مجموعة لها عنوان + عدّاد أنواع. كل **كارت حبة** (`rounded-2xl`، `min-h-[15.5rem]`):
  - خلفية تدرّج داكن. حالات: عادي (حدّ ذهبي خفيف + يرتفع عند hover) / مختار (`border-[#D6A373]/55` + توهّج + خلفية ذهبية) / نافد (`opacity-45 grayscale` + كبسولة `Out of Stock`).
  - زاوية علوية: توهّج دائري `blur-xl` + دائرة اختيار (تتحوّل ذهبية ممتلئة بعلامة `Check` عند الاختيار، وإلا `ChevronRight`).
  - اسم (Serif) + وصف (سطرين) + **إيحاءات تذوق** (كبسولات صغيرة) + **3 أشرطة مؤشرات** (`IndicatorBar`: القوام/الحموضة/الكريما — شريط ذهبي يتحرك عرضه على `500ms`) + صف المنشأ والسعر.
  - في وضع Custom: حقل رقمي `%` لإدخال النسبة.
- **لوحة المعاينة الحية** (`LiveConfiguratorPanel`، لاصقة `sticky top-28`): تعرض السعر الإجمالي بخط Serif ذهبي ضخم `text-4xl` يتغيّر مع كل تعديل، + ملخص الحبوب المختارة (كبسولات بأزرار حذف `X`) + اختيار الوزن (`250g/500g/1kg`) + عدّاد كمية (أزرار `Minus/Plus` دائرية) + رسائل خطأ (لو النسب ≠ 100% أو نفاد مخزون) + زر `premium-button` كامل العرض `Add to Cart` بأيقونة `ShoppingBag`.

### 3.3 — أداة "اصنع نكهتك" (Customize Flavor)

نفس بنية أداة الإسبريسو لكن للنكهات: تختار **قاعدة** (تركي/كابتشينو/هوت شوكولت/كوفي ميكس) ثم **إضافات نكهة** مقسّمة لمجموعات (حلويات/مكسرات/فواكه/طلبات خاصة). نفس نظام الكروت، المؤشرات، ولوحة المعاينة الحية، وحساب السعر الديناميكي.

### 3.4 — مكوّنات الكونفيجريتر المشتركة — `premium-configurator.tsx`

أربع لبنات معاد استخدامها في كل الأدوات:
- **`PremiumConfiguratorShell`**: قشرة `luxury-panel` بحواف `rounded-2xl`. رأس به دائرة أيقونة `56px` ذهبية + Eyebrow (كابيتال متباعد) + عنوان Serif ضخم `text-3xl/4xl` + وصف.
- **`PremiumOptionCard`**: كارت خيار `min-h-36`، خلفية تدرّج داكن، 3 حالات (عادي يرتفع عند hover / مختار بتوهّج / معطّل `grayscale`)، توهّج زاوية + دائرة اختيار (Check/ChevronRight).
- **`IndicatorBar`**: شريط مؤشّر أفقي بقيمة `value/max`، تعبئة ذهبية تتحرّك على `500ms ease-out`.
- **`LiveConfiguratorPanel`**: لوحة جانبية لاصقة بسعر ذهبي ضخم متحرّك + ملاحظة + محتوى.

### 3.5 — صفحة تفاصيل المنتج — `app/products/[slug]/page.tsx` + `product-detail.tsx`

- **مسار التنقّل (Breadcrumb):** الرئيسية ← المنتجات ← الفئة ← اسم المنتج (مفصولة بأيقونات `ChevronRight`).
- **التخطيط:** شبكة عمودين على `lg` (صور | تفاصيل)، فجوة `32–48px`.
- **عمود الصور:**
  - **صورة رئيسية** بنسبة `4/5` (بورتريه سينمائي)، `premium-image-card`، حواف `rounded-2xl`. تتبدّل بـ `AnimatePresence` (crossfade `opacity 0.3s`) و`layoutId` للانتقال السلس. فلتر سينمائي `brightness-[0.82] contrast-[1.08] saturate-[1.05]` + طبقتا تظليل (سفلي + صبغة دافئة `mix-blend-multiply`).
  - **شارات أعلى يسار:** `New / جديد` (بيج) · `Best Seller` (بني) · `-N%` (أحمر).
  - **شريط مصغّرات (Thumbnails):** صور `80×80` قابلة للنقر، المختارة بحدّ ذهبي وتوهّج، الباقي باهت يتفتّح عند hover.
- **عمود التفاصيل (مسافات رأسية `space-y-6`):**
  - صف المنشأ + تقييم (نجمة ممتلئة + `4.8` + `128 reviews`).
  - **العنوان** (Serif، `text-3xl/4xl`، `text-balance`) + وصف (`text-pretty`).
  - **درجة التحميص** (Badge) + **ملاحظات النكهة** (Badges متعدّدة).
  - **اختيار الحجم** (`RadioGroup`): كبسولات `250g/500g/1kg` كل واحدة تعرض الحجم + السعر + السعر قبل الخصم (مشطوب). المختارة بحدّ ذهبي وتوهّج `ring`.
  - **حالة المخزون:** كبسولة بنقطة ملوّنة — أحمر `Out of Stock` / كهرماني `Only X left` / أخضر `In Stock`.
  - **عدّاد الكمية** (أزرار `Minus/Plus` دائرية محدودة بالمخزون).
  - **السعر** (`text-3xl` ذهبي يتغيّر مع الكمية + سعر مشطوب + نسبة التوفير) + **زر `premium-button` دائري `Add to Cart`** + زر مفضلة (قلب يمتلئ) + زر مشاركة.
  - **3 كروت ضمانات** (`premium-info-card`): توصيل سريع (Truck) · تحميص طازج (Coffee) · دعم 24/7 (Headphones).
- **أسفل الصفحة:** قسم **منتجات ذات صلة** (`RelatedProducts`) بنفس شبكة `ProductCard`.

---

## القسم 4 — صفحات المحتوى

### 4.1 — صفحة "من نحن" (About) — `app/about/page.tsx`

صفحة بسيطة تجمع مكوّنين، خلفية `#0B0806`، محتواهما يُجلب من نظام الميديا (`about_top` و `about_story`) مع نصوص احتياطية:
- **`AboutHero`:** هيرو بارتفاع `55vh` يمتد تحت الهيدر. صورة `object-cover` فوقها **خمس طبقات تظليل متراكبة** (أسود `60%` + صبغة `mix-blend-multiply` + vignette شعاعي + تدرّج سفلي + توهّج ذهبي مركزي). محتوى وسط (تظهر بحركة `framer-motion` تصاعدية متدرّجة): Eyebrow `Since 2019 / منذ 2019` + عنوان Serif ضخم `text-4xl→6xl` بظل + جملة فرعية.
- **`AboutJourney`:** قسم قصة بشبكة عمودين على `md`. العمود الأيسر (يدخل من اليسار): خط ذهبي + Eyebrow `Our Journey / رحلتنا` + عنوان Serif + فقرات نص (تنقسم تلقائياً على أسطر فارغة). العمود الأيمن: صورة مربّعة (`aspect-square`) لاصقة `sticky top-28` بحواف `rounded-2xl` وظل عميق + تدرّجات فوقها (تدخل من اليمين).
- يوجد أيضاً مكوّنات `about-stats.tsx` و `about-values.tsx` متاحة لكن الصفحة الحالية تستخدم الهيرو والقصة فقط.

### 4.2 — صفحة المدوّنة (Blog) — `app/blog/page.tsx` + `app/blog/[slug]/page.tsx`

- **القائمة:** خلفية `#0B0806` + `BlogHero`. تجلب التدوينات من `/api/blog/public` (مع 3 تدوينات احتياطية لو فارغ). شبكة عمودين على `md`، فجوة `24px`.
- **كارت تدوينة** (`rounded-2xl`، حدّ ذهبي): صورة غلاف بارتفاع `160–192px` (`object-cover` تكبر `scale-105` عند hover) + تدرّج سفلي. عند الـ hover الكارت يرتفع `-translate-y-1` ويظهر توهّج وظل. المحتوى: تاريخ (أيقونة `Calendar`، يُنسّق بالعربي/الإنجليزي) + عنوان Serif (سطرين كحد أقصى، يتحوّل ذهبي عند hover) + مقتطف (3 أسطر، يُجرَّد من وسوم HTML) + رابط `Read more / اقرأ المزيد` بسهم يتحرّك (ينعكس في RTL).
- **حالة التحميل:** 4 كروت skeleton (`animate-pulse`). **حالة الفراغ:** `BlogEmptyState`.

### 4.3 — صفحة التواصل (Contact) — `app/contact/page.tsx`

> ملاحظة: هذه صفحة مستقلّة تختلف عن قسم التواصل في الصفحة الرئيسية (2.10).

- **خلفية ملء الشاشة:** صورة هيرو (تُجلب من `contact_page` مع صورة احتياطية من Unsplash) `object-cover` + فلاتر بصرية ديناميكية (`buildEffectsFilter`) + طبقة تظليل (`buildOverlayGradient`) + طبقة `#0B0806/38` + حبيبات (grain) شرطية + توهّج شعاعي.
- **الرأس** (وسط، `pt-32→40`): خطّان ذهبيان حول Eyebrow `Get In Touch / تواصل معنا` + عنوان Serif `text-4xl/5xl` `Contact Us / تواصل معنا` + فقرة.
- **شبكة عمودين على `lg`:**
  - **عمود المعلومات** (يدخل من اليسار): 4 كروت تواصل (خلفية زجاجية `backdrop-blur` + حدّ ذهبي): العنوان (`MapPin`) · الهاتف (`Phone`، رابط اتصال) · الإيميل (`Mail`، mailto) · واتساب (`MessageCircle`، رابط wa.me). الكروت ذات الروابط تكبر `scale-[1.01]` عند hover.
  - **عمود الفورم** (يدخل من اليمين، كارت زجاجي `rounded-2xl`): عنوان `Send us a Message / أرسل لنا رسالة` + حقول (الاسم + الإيميل في صف، ثم الموضوع، ثم الرسالة) بخلفية ذهبية شفّافة وحدّ ذهبي + **تحقق فوري** (يُظهر أخطاء حمراء تحت كل حقل ويمرّر لأول حقل خاطئ) + زر تدرّج ذهبي `Send Message / إرسال الرسالة` (يتحوّل لـ spinner دائري أثناء الإرسال). يرسل لـ `/api/contact` ثم يفتح واتساب.

### 4.4 — صفحة كتابة مراجعة (Reviews) — `app/reviews/page.tsx`

- خلفية `#0B0806` + توهّج شعاعي علوي. حاوية ضيّقة (`max-w-xl`).
- رابط رجوع `Back / رجوع` (سهم ينعكس في RTL) + رأس وسط: Eyebrow `Share Your Experience / شارك تجربتك` + عنوان Serif `Write a Review / اكتب مراجعتك` + فقرة.
- **الفورم** (`rounded-2xl`، حدّ ذهبي): الاسم (مطلوب) + **منتقي نجوم تفاعلي** (`StarSelector`: 5 نجوم تمتلئ ذهبية عند hover/الاختيار، تكبر عند hover، مع نص وصفي `Excellent…Poor`) + مراجعة بالإنجليزي (مطلوب، عدّاد `/1000`) + مراجعة عربية اختيارية + زر دائري ذهبي `Submit Review / إرسال المراجعة` (يرتفع ويتوهّج عند hover، معطّل لو الحقول ناقصة).
- **رسالة خطأ** حمراء عند الفشل. **شاشة نجاح** كاملة (أيقونة `CheckCircle` بدائرة ذهبية + شكر + ملاحظة أنها بانتظار الاعتماد + زر `Back to Home`).
- يرسل لـ `/api/reviews`؛ المراجعات تُعتمَد يدوياً قبل الظهور.

### 4.5 — صفحة تتبّع الطلب (Track) — `app/track/page.tsx`

- **الرأس:** خلفية تدرّج داكن (أو صورة `track_page` مع فلاتر/تظليل/حبيبات) + شعار `logo-white.svg` + عنوان `Track Your Order / تتبع طلبك` + جملة فرعية.
- **صندوق البحث** (`rounded-2xl`، حدّ ذهبي): حقل رقم الطلب (يقبل Enter) + زر `premium-button` `Track / تتبع` (spinner أثناء التحميل).
- **النتيجة:**
  - **كارت ملخّص الطلب:** رقم الطلب (Serif) + تاريخ + الإجمالي الذهبي + **خط زمني للحالة (Timeline)**: 5 خطوات (`pending → confirmed → preparing → shipped → delivered`) كل خطوة دائرة أيقونة، المكتملة بتدرّج ذهبي وتوهّج، **الحالية لها نبض `animate-ping`**، وخط تقدّم يمتلئ بتدرّج ذهبي بعرض متناسب (انتقال `700ms`). لو ملغى: كارت أحمر `Order Cancelled / تم إلغاء الطلب` بسبب الإلغاء.
  - **كارت محتويات الطلب:** قائمة عناصر (شارة `Custom / مخصص` للمنتجات المخصّصة + الاسم + الحجم/الكمية + تفاصيل التخصيص + السعر) + ملخّص توصيل + إجمالي.
  - **CTA مراجعة** عند التسليم (`delivered`): دعوة لكتابة مراجعة برابط `/reviews`.
  - **رابط مساعدة واتساب** أسفل النتيجة.
- **حالات:** skeleton أثناء التحميل + كارت خطأ أحمر (`XCircle`) لو الطلب غير موجود.

### 4.6 — الصفحات القانونية — `privacy-policy` و `terms-of-use`

صفحتان نصّيتان بسيطتان (Server Components، بدون تفاعل). خلفية `#0B0806`، حاوية `max-w-3xl`: Eyebrow `Line Coffee` + عنوان Serif `text-4xl` + فقرات نص بيج بمسافة سطور مريحة `leading-7`. حالياً **نصوص مؤقتة (placeholder)** بانتظار ربطها بـ CMS قانوني لاحقاً.

---

## القسم 5 — صفحات الحساب (Auth) ولوحة العميل

### 5.1 — تسجيل الدخول — `app/auth/login/page.tsx`

- مغلّفة بـ `<Suspense>` (تقرأ `searchParams` مثل `next`/`redirect`/`message`). تدخل بحركة `framer-motion` تصاعدية، حاوية وسط `max-w-md`.
- رأس وسط: عنوان Serif `Welcome Back / مرحباً بعودتك` + جملة فرعية.
- **فورم:** حقل الإيميل (أيقونة `Mail` يسار) + حقل كلمة المرور (أيقونة `Lock` + زر إظهار/إخفاء `Eye/EyeOff`) + رابط `Forgot password? / نسيت كلمة المرور؟`. **تحقق فوري** يُظهر أخطاء حمراء ويمرّر لأول حقل خاطئ. زر `Sign In / تسجيل الدخول` كامل العرض بسهم (ينعكس RTL، يتحوّل spinner أثناء التحميل).
- رابط أسفل `Don't have an account? Sign up / سجل الآن`.
- منطق: يستخدم Server Action `signIn`. الأدمن يُحوَّل لـ `/dashboard/admin`، الباقي للمسار المطلوب (`sanitizeRedirectPath`). toast نجاح/فشل.

### 5.2 — إنشاء حساب — `app/auth/signup/page.tsx` (400 سطر)

نفس النمط البصري للدخول لكن فورم أطول مع تحقق شامل لكل الحقول: الاسم الأول/الأخير + الإيميل + الهاتف + الواتساب + العنوان + كلمة المرور. رسائل خطأ ثنائية اللغة لكل حقل. بعد التسجيل يُحوّل (غالباً لرسالة `check-email`).

### 5.3 — نسيت/إعادة تعيين كلمة المرور والخطأ

- **`forgot-password`:** فورم بسيط (الإيميل) + **شاشة نجاح** `Check Your Email / تحقق من بريدك` بعد الإرسال + رابط `Back to Login`.
- **`reset-password`:** فورم كلمة مرور جديدة + تأكيد (تحقق: 6 أحرف+، تطابق). **شاشة "الرابط منتهي أو غير صالح"** مع زر `Request New Link` لو الرمز غير صحيح. نجاح `Password updated successfully`.
- **`error`:** صفحة صغيرة (18 سطر) لرسالة `Authentication link expired`.

### 5.4 — حماية لوحة العميل — `app/dashboard/layout.tsx`

Layout بسيط `force-dynamic` يستدعي `requireUser('/dashboard')` (يحوّل غير المسجّلين لتسجيل الدخول مع `next`). يلفّ كل صفحات لوحة العميل.

### 5.5 — لوحة العميل الرئيسية — `app/dashboard/page.tsx`

- محمية على مستوى العميل: لو أدمن يُحوَّل لـ `/dashboard/admin`، لو غير مسجّل لـ login. spinner أثناء `isLoading`. حشوة علوية كبيرة (`pt-32→40`) لتفادي الهيدر.
- رأس: عنوان Serif `My Account / حسابي` + `Welcome back, {firstName}`.
- **شبكة 3 أعمدة على `md`** (تدخل بحركة متدرّجة):
  - **كارت البروفايل** (عمود واحد، `bg-card`): دائرة أفاتار `80px` بأحرف الاسم الأولى (نص primary) + الاسم + الإيميل + زر `Edit Profile` + زر `Sign Out` (أحمر/destructive).
  - **قائمة التنقّل** (عمودان، كارت مقسّم بحدود): روابط بأيقونة دائرية primary + اسم + سهم `ChevronRight` (ينعكس RTL): ملفي · طلباتي · المفضلة · العناوين · الإعدادات. + رابط إضافي **`Admin Dashboard`** (أيقونة `ShieldCheck`) يظهر للأدمن فقط.
  - **معاينة الطلبات الأخيرة** (أسفل القائمة): عنوان + `View All` + أحدث 3 طلبات (رقم + تاريخ + إجمالي + حالة) تُجلب من `/api/orders`. spinner أثناء التحميل. **حالة فراغ** (`Package` باهت + `No orders yet` + زر `Start Shopping`).

### 5.6 — صفحات لوحة العميل الفرعية

كلها تبدأ برابط رجوع `Back to Dashboard / العودة للوحة التحكم` + عنوان Serif `text-3xl` + جملة فرعية، بنفس لغة الكروت (`bg-card`، حدود، خلفية داكنة):
- **`orders`** (435 سطر): قائمة كل الطلبات بحالاتها (Pending/Confirmed/Preparing/Shipped/Delivered)، طرق دفع (`Cash on Delivery` / `Electronic Wallet` / `InstaPay`)، تفاصيل العناصر والتخصيصات.
- **`profile`** (299 سطر): فورم تعديل البيانات (الاسم الأول/الأخير، الهاتف…) + حفظ مع toast نجاح/فشل.
- **`addresses`**: عرض/تعديل عنوان التوصيل الافتراضي (رابط خرائط جوجل) + زر `Add/Edit Address`، حالة فراغ.
- **`wishlist`**: شبكة المنتجات المحفوظة (من `useWishlistStore`) + زر `Clear Wishlist` + حالة فراغ (`Shop Products`).
- **`settings`**: تبديل اللغة + قسم الحساب (روابط `Edit Profile` و `Sign Out`).

---

## القسم 6 — لوحة تحكم الأدمن

> **ملاحظة مهمة عن الهوية البصرية:** لوحة الأدمن تستخدم **لوحة ألوان أغمق ومختلفة قليلاً** عن واجهة المتجر: خلفيات شبه سوداء (`#080503` / `#0f0900` / `#180d04`)، ذهبي بدرجة `#c8941a` (بدل `#D6A373` في المتجر)، ونصوص بيضاء بشفافيات متدرّجة (`white/90`, `white/40`, `white/25`). الحالات الملوّنة (كهرماني/أخضر/أزرق/نيلي/أحمر) تُستخدم لشارات حالة الطلبات.

### 6.1 — هيكل اللوحة — `app/dashboard/admin/layout.tsx`

تخطيط أدمن كامل (Client Component) يمتد تحت الهيدر العام (`-mt-20/-mt-24`) ويجبر `colorScheme: dark`:
- **شريط جانبي ثابت** (`fixed`، عرض `215px`، يظهر على `md`+): خلفية `#080503/96` مع `backdrop-blur` وحدّ ذهبي + خط ذهبي عمودي متدرّج. أعلاه شعار `logo-white.svg` + لافتة `Admin Panel / لوحة الإدارة`. ثم قائمة 13 رابط (نظرة عامة، الطلبات، المنتجات، الفئات، أنواع القهوة، النكهات، العملاء، رسائل التواصل، المراجعات، الخصومات، المدونة، الوسائط، الإعدادات) كل رابط بأيقونة. **الرابط النشط** بحدّ ذهبي وخلفية تدرّج ذهبي/بني ونص ذهبي وظل. رابط الطلبات يحمل **شارة عدّاد حمراء** للطلبات المعلّقة.
- **شريط علوي ثابت** (`fixed`، ارتفاع `64px`، يبدأ بعد الشريط الجانبي): عنوان الصفحة الحالية (يُشتق من `pageTitles`)، وفي الصفحة الرئيسية `Welcome back 👋` + جملة. يمين الشريط: حقل بحث (يظهر على `sm`+) + زر **تبديل اللغة** (`EN/AR`) + **جرس إشعارات** (شارة عدد + قائمة منسدلة بالطلبات المعلّقة مع حالة فراغ ورابط "عرض الكل") + زر إعدادات + **قائمة أفاتار منسدلة** (أحرف الاسم الأولى + اسم/إيميل + روابط Dashboard/View Store/Sign Out).
- **شريط تنقّل أفقي للموبايل** (يظهر بدل الجانبي على الموبايل): كبسولات أفقية قابلة للتمرير لنفس روابط القائمة، اللاصق `sticky top-16`.
- **خلفية المحتوى:** تدرّج `#120D09→#0B0806→#0f0900` + توهّج شعاعي ثابت.
- **التحديث الحي:** يجلب عدد الطلبات المعلّقة من `/api/admin/orders` كل 60 ثانية.

### 6.2 — صفحة النظرة العامة — `app/dashboard/admin/page.tsx` (488 سطر)

تستخدم **Recharts** للرسوم. تجلب كل البيانات من `/api/admin/stats`:
- **لافتة ترحيب** (`rounded-2xl`، تدرّج داكن + توهّج + خط ذهبي علوي): شعار + جملة + **الإيراد المحصّل** (رقم ذهبي ضخم) + التاريخ الكامل.
- **4 كروت إحصائية** (`StatCard`، شبكة 2/4 أعمدة): كل كارت أيقونة ذهبية بإطار + قيمة ضخمة + تسمية + **نسبة تغيّر** (سهم أخضر صاعد / أحمر هابط + `vs last month`). يرتفع `-translate-y-0.5` ويتوهّج عند hover. الكروت: إجمالي المبيعات، **الطلبات النشطة** (كارت خاص `ActiveOrdersCard` يفصّل: مؤكد/قيد التجهيز/تم الشحن + إجمالي المبالغ المعلّقة)، إجمالي العملاء، الطلبات الملغاة.
- **صف 2:** **رسم المبيعات** (`AreaChart` بتدرّج ذهبي `goldGrad`، آخر 30 يوم، Tooltip مخصّص داكن) يأخذ عمودين + **كارت آخر الطلبات** (قائمة برقم/اسم/تاريخ + `StatusBadge` ملوّن حسب الحالة + المبلغ، مع حالة فراغ).
- **صف 3:** **الفئات الشعبية** (شبكة كروت صور + عدد منتجات) + **دونات العملaء** (`PieChart` بحلقة جدد/عائدون + إجمالي في المنتصف + مفتاح ألوان) + **أفضل المنتجات** (قائمة بصور ومبيعات).
- **حالات:** skeletons (`Sk`، `animate-pulse`) أثناء التحميل + كارت خطأ أحمر.

### 6.3 — صفحات الإدارة الفرعية (نمط مشترك)

كل صفحات الإدارة تشترك في نفس اللغة البصرية الداكنة: كروت `#180d04`/`#120D09` بحدّ ذهبي `#c8941a/10`، جداول/قوائم صفوف بفواصل `white/[0.03]`، شارات حالة ملوّنة، أزرار ذهبية، حقول بحث/فلترة داكنة، نوافذ (modals/drawers) للإضافة والتعديل، وحالات تحميل (skeletons) وفراغ. أبرزها:
- **`orders`** (المنتج 704 سطر تقريباً): جدول/كروت الطلبات مع فلترة بالحالة، تفاصيل كل طلب (عناصر، تخصيصات، عنوان، دفع)، وتحديث الحالة عبر خطوات (`pending→…→delivered`) أو إلغاء.
- **`products`**: إدارة المنتجات (CRUD) — قائمة بصور وأسعار ومخزون، نموذج إضافة/تعديل (أحجام، أسعار، خصومات، صور، فئة، درجة تحميص، ملاحظات نكهة)، تبديل الإتاحة/النفاد.
- **`categories`**: CRUD الفئات (اسم ثنائي اللغة، slug، صورة).
- **`coffee-beans`** و **`flavors`**: إدارة خيارات التخصيص (حبوب البن وإضافات النكهة) المستخدمة في أدوات "اصنع إسبريسو/نكهتك" — أسعار، مخزون، إتاحة لكل قاعدة، رؤية.
- **`customers`**: قائمة العملاء وبياناتهم وطلباتهم.
- **`contact-messages`**: عرض رسائل فورم التواصل وحالتها.
- **`reviews`**: اعتماد/رفض المراجعات الواردة قبل ظهورها في المتجر.
- **`discounts`**: إدارة أكواد/قواعد الخصم.
- **`blog`**: CRUD تدوينات المدوّنة (عنوان، slug، غلاف، محتوى، نشر).
- **`banners`** (مدير الوسائط): رفع/تعيين صور أقسام الموقع (الهيرو، البانرات، صور الأقسام) مع تأثيرات بصرية (فلاتر، تظليل، حبيبات) — مصدر بيانات `/api/media`.
- **`settings`**: إعدادات المتجر العامة (التواصل، واتساب، الشحن…) و **`analytics`**: تحليلات أعمق للمبيعات والأداء.

---

## القسم 7 — مرجع التأثيرات والحركة والأشكال (Design System)

هذا القسم يلخّص نظام التصميم المشترك (من `app/globals.css`) الذي يجب الالتزام به في أي نسخة جديدة لإعادة إنتاج نفس الإحساس "الفاخر السينمائي".

### 7.1 — الألوان (CSS Variables)

تُعرَّف كـ متغيّرات في `globals.css`:
- `--color-coffee-black: #0B0806` (الخلفية الأساسية) · `--color-coffee-deep: #120D09` · `--color-coffee-dark: #15100B` · `--color-coffee-surface: #1B140F`.
- `--color-gold: #B6885E` · `--color-gold-light: #D6A373` (الذهبي الأساسي للمتجر).
- `--color-cream: #F5E6D8` (النص الرئيسي) · `--color-cream-muted: #D6B79A` · `--color-cream-dim: #B79B85`.
- **لوحة الأدمن** تستثني هذا وتستخدم ذهبي أغمق `#c8941a` وخلفيات `#080503/#0f0900/#180d04` (انظر 6).

### 7.2 — الخطوط (Typography)

- `--font-sans` و `--font-serif` كلاهما = **Playfair Display** (`--font-playfair`) — لذا العناوين والنصوص الأساسية كلها Serif فاخر (هذا مقصود).
- `--font-arabic-brand` = **Cairo** (`--font-cairo`) — يُطبّق على العربية عبر كلاس `.font-arabic` ومحدّدات اللغة.
- منحنى الحركة الموحّد: `--ease-luxury: cubic-bezier(0.22, 1, 0.36, 1)` (نهاية ناعمة "ease-out").
- `--radius: 0.625rem` (أساس الحواف؛ معظم الكروت `rounded-2xl`).

### 7.3 — كلاسات المكوّنات الجاهزة (Utility Classes)

| الكلاس | الوصف |
|--------|-------|
| `.luxury-panel` | لوحة بتدرّج داكن `145deg` + حدّ ذهبي `0.16` + ظل عميق `70px` + إضاءة داخلية علوية + `backdrop-blur(18px)`. |
| `.luxury-card` | كارت منتج — يرتفع ويتوهّج عند hover (انتقال `520ms`). |
| `.premium-button` | زر تدرّج ذهبي `135deg (#A8744E→#D6A373→#B6885E)`، نص داكن، ظل ذهبي؛ عند hover يرتفع `-2px` + يزيد التشبّع والظل (`420ms`). |
| `.premium-button-outline` | زر شفّاف بحدّ ذهبي + `backdrop-blur`؛ يرتفع ويتوهّج عند hover. |
| `.premium-image-card` | إطار صورة بحدّ ذهبي خفيف + ظل؛ عند hover يرتفع `-5px` + توهّج ذهبي (`520ms`). |
| `.premium-info-card` | كارت معلومات صغير بتدرّج خفيف؛ يرتفع `-4px` عند hover. |
| `.premium-section-kicker` | Eyebrow صغير بخطّين ذهبيّين جانبيّين (`::before/::after`). |
| `.premium-heading-shimmer` | عنوان بـ "وميض" ذهبي يمرّ كل `6.4s`. |
| `.text-gradient` | نص بتدرّج ذهبي `#B6885E→#D6A373→#F5E6D8` (clip للنص). |
| `.glass` / `.nav-glass` | تأثيرات زجاجية للهيدر والعناصر العائمة. |
| `.cinematic-section` | قسم بطبقات تظليل علوية/سفلية (`::before/::after`). |
| `.nav-link` / `.nav-link-active` / `.nav-sweep` | روابط الهيدر مع تأثير "كنس" ذهبي للنشط. |
| `.torn-paper` / `.torn-paper-top` (+ `-dark`) | حواف "ورق ممزّق" SVG (للفصل بين الأقسام)؛ النسخة الداكنة بلون `#120D09`. |
| `.scroll-progress` | شريط تقدّم تمرير علوي `2px` بتدرّج ذهبي، `z-9999`. |
| `.custom-cursor` / `.cursor-dot` / `.cursor-ring` | مؤشّر مخصّص (نقطة + حلقة تكبر عند hover). |
| `.image-reveal` / `.marquee` / `.gold-pulse` / `.parallax-slow` | حركات: كشف الصور، شريط متحرّك (يتوقّف عند hover)، نبض ذهبي `4s`، بارالاكس. |
| `.loader-*` | عناصر شاشة التحميل (بخار، توهّج، شعار، شريط تقدّم) — انظر القسم 1. |

### 7.4 — الحركة (Motion)

- **مكتبة:** `framer-motion` في معظم الصفحات للظهور التدريجي.
- **النمط السائد:** `initial={{ opacity: 0, y: 20 }}` → `animate/whileInView={{ opacity: 1, y: 0 }}` مع `transition={{ delay }}` متدرّج للعناصر المتتابعة، و`viewport={{ once: true }}` للأقسام التي تظهر عند التمرير. الأعمدة الجانبية تدخل من اليسار/اليمين (`x: ±20`).
- **انتقالات CSS:** كلها تستخدم `--ease-luxury` بمدد `340–520ms`.
- **حركات مفتاحية (keyframes):** `premium-heading-sweep` (وميض العنوان)، `nav-link-sweep`، `marquee`، `goldPulse`، `imageReveal`، إضافة لـ `animate-ping`/`animate-pulse`/`animate-spin` من Tailwind (نبض خطوة التتبّع، skeletons، spinners).
- **إمكانية الوصول:** يوجد `@media (prefers-reduced-motion: reduce)` يوقف الأنيميشن المتكرّرة.

### 7.5 — الأشكال والطبقات (Shapes & Layering)

- **الحواف:** الكروت `rounded-2xl`، الكبسولات/الشارات `rounded-full`، الحقول `rounded-xl`.
- **التظليل السينمائي للصور:** نمط متكرّر = صورة `object-cover` بفلتر `brightness/contrast/saturate` + **عدّة طبقات متراكبة**: تدرّج سفلي (نحو الأسود)، صبغة دافئة `mix-blend-multiply`، vignette شعاعي، وتوهّج ذهبي شعاعي خفيف.
- **التوهّجات (Glows):** دوائر `blur-xl/blur-2xl` بلون ذهبي شفّاف في زوايا الكروت، تشتدّ عند hover.
- **الحبيبات (Grain):** `GRAIN_SVG` بـ `mix-blend-screen` وشفافية منخفضة على صور الأقسام (حسب إعدادات الميديا).
- **التركيز (Focus):** `:focus-visible` بحدّ ذهبي `2px`. **التحديد (Selection):** خلفية ذهبية شفّافة ونص كريمي.
- **القاعدة الذهبية للتباين:** خلفيات داكنة جداً + نص كريمي/ذهبي؛ أي تغيير لخلفية عنصر يستوجب تغيير لون نصّه للحفاظ على التباين.
